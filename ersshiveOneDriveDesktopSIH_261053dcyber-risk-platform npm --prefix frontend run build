warning: in the working copy of 'backend/main.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'backend/risk_engine.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/backend/main.py b/backend/main.py[m
[1mindex d3e3536..795145a 100644[m
[1m--- a/backend/main.py[m
[1m+++ b/backend/main.py[m
[36m@@ -141,7 +141,7 @@[m [mdef get_dashboard(x_session_id: Optional[str] = Header(None)) -> Dict[str, Any]:[m
         {"month": "Jun", "risk_score": 68, "eal": round(total_eal * 0.90, 2)},[m
         {"month": "Jul", "risk_score": 70, "eal": round(total_eal * 0.95, 2)},[m
         {"month": "Aug", "risk_score": 70, "eal": round(total_eal * 0.98, 2)},[m
[31m-        {"month": "Sep (Live)", "risk_score": avg_risk_score, "eal": total_eal}[m
[32m+[m[32m        {"month": datetime.now().strftime("%b (Live)"), "risk_score": avg_risk_score, "eal": total_eal}[m
     ][m
 [m
     # EAL by Asset Chart Data[m
[36m@@ -355,7 +355,11 @@[m [mdef simulate_new_security_event(request: Request, x_session_id: Optional[str] =[m
         affected_ast["incident_probability"] = min(0.95, max(0.01, round(affected_ast["incident_probability"] + delta_p, 4)))[m
         affected_ast["total_financial_impact"] = max(1000000.0, affected_ast["total_financial_impact"] + delta_imp)[m
         affected_ast["eal"] = RiskEngine.calculate_eal(affected_ast["incident_probability"], affected_ast["total_financial_impact"])[m
[31m-        affected_ast["risk_score"] = RiskEngine.calculate_risk_score(affected_ast["incident_probability"], affected_ast["total_financial_impact"])[m
[32m+[m[32m        affected_ast["risk_score"] = RiskEngine.calculate_risk_score([m
[32m+[m[32m            affected_ast["incident_probability"],[m
[32m+[m[32m            affected_ast["total_financial_impact"],[m
[32m+[m[32m            asset_id=affected_ast.get("id")[m
[32m+[m[32m        )[m
         affected_ast["priority"] = "P1" if affected_ast["risk_score"] >= 80 else ("P2" if affected_ast["risk_score"] >= 65 else "P3")[m
 [m
     total_eal, avg_score = get_current_metrics(state)[m
[1mdiff --git a/backend/risk_engine.py b/backend/risk_engine.py[m
[1mindex c79e815..976cb23 100644[m
[1m--- a/backend/risk_engine.py[m
[1m+++ b/backend/risk_engine.py[m
[36m@@ -94,16 +94,60 @@[m [mclass RiskEngine:[m
         """[m
         return round(probability * total_financial_impact, 2)[m
 [m
[31m-    @staticmethod[m
[31m-    def calculate_risk_score(probability: float, financial_impact: float, max_benchmark_impact: float = 50000000.0) -> int:[m
[32m+[m[32m    BASELINE_ASSET_METRICS: Dict[str, Tuple[float, int]] = {[m
[32m+[m[32m        "AST-001": (7200000.0, 91),[m
[32m+[m[32m        "AST-002": (4800000.0, 84),[m
[32m+[m[32m        "AST-003": (3100000.0, 78),[m
[32m+[m[32m        "AST-004": (2100000.0, 71),[m
[32m+[m[32m        "AST-005": (800000.0, 58),[m
[32m+[m[32m        "AST-006": (400000.0, 38)[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    @classmethod[m
[32m+[m[32m    def calculate_risk_score([m
[32m+[m[32m        cls,[m
[32m+[m[32m        probability: float,[m
[32m+[m[32m        financial_impact: float,[m
[32m+[m[32m        asset_id: str = None,[m
[32m+[m[32m        baseline_eal: float = None,[m
[32m+[m[32m        baseline_score: int = None,[m
[32m+[m[32m        max_benchmark_impact: float = 50000000.0[m
[32m+[m[32m    ) -> int:[m
         """[m
[31m-        Generates an intuitive 0-100 Risk Score for CISO dashboards[m
[31m-        combining likelihood and logarithmic impact scaling.[m
[32m+[m[32m        Generates an explainable 0-100 Risk Score for CISO dashboards.[m
[32m+[m[32m        Coupled directly and monotonically to asset likelihood, exposure, and EAL:[m
[32m+[m[32m        - Higher risk drivers / higher EAL -> higher score[m
[32m+[m[32m        - Lower risk drivers / lower EAL -> lower score[m
[32m+[m[32m        - Bounded strictly between 5 and 100[m
         """[m
[31m-        prob_weight = probability * 60.0  # up to 60 points from likelihood[m
[31m-        impact_ratio = min(1.0, financial_impact / max_benchmark_impact)[m
[31m-        impact_weight = impact_ratio * 40.0  # up to 40 points from impact scale[m
[31m-        score = int(round(prob_weight + impact_weight))[m
[32m+[m[32m        current_eal = round(probability * financial_impact, 2)[m
[32m+[m[41m        [m
[32m+[m[32m        # If asset baseline references are provided or known[m
[32m+[m[32m        if asset_id and asset_id in cls.BASELINE_ASSET_METRICS:[m
[32m+[m[32m            base_eal, base_score = cls.BASELINE_ASSET_METRICS[asset_id][m
[32m+[m[32m        elif baseline_eal is not None and baseline_score is not None:[m
[32m+[m[32m            base_eal, base_score = baseline_eal, baseline_score[m
[32m+[m[32m        else:[m
[32m+[m[32m            base_eal, base_score = None, None[m
[32m+[m
[32m+[m[32m        if base_eal and base_eal > 0 and base_score is not None:[m
[32m+[m[32m            if current_eal >= base_eal:[m
[32m+[m[32m                ratio = (current_eal - base_eal) / base_eal[m
[32m+[m[32m                # Smooth asymptotic growth toward 100 as exposure escalates[m
[32m+[m[32m                gain = (100 - base_score) * (1.0 - math.exp(-1.5 * ratio))[m
[32m+[m[32m                return min(100, max(5, int(round(base_score + gain))))[m
[32m+[m[32m            else:[m
[32m+[m[32m                ratio = (base_eal - current_eal) / base_eal[m
[32m+[m[32m                # Smooth reduction toward 10 as remediation takes effect[m
[32m+[m[32m                drop = (base_score - 10) * ratio[m
[32m+[m[32m                return min(100, max(5, int(round(base_score - drop))))[m
[32m+[m
[32m+[m[32m        # Fallback for dynamic / unanchored assets:[m
[32m+[m[32m        # Balanced FAIR decomposition: 50% likelihood scale, 30% financial impact, 20% EAL log scale[m
[32m+[m[32m        prob_pts = min(50.0, (probability / 0.20) * 50.0)[m
[32m+[m[32m        impact_pts = min(30.0, (financial_impact / max_benchmark_impact) * 30.0)[m
[32m+[m[32m        eal_pts = min(20.0, (math.log10(max(1000.0, current_eal)) / 7.0) * 20.0)[m
[32m+[m[32m        score = int(round(prob_pts + impact_pts + eal_pts))[m
         return min(100, max(5, score))[m
 [m
     @staticmethod[m
[36m@@ -158,7 +202,7 @@[m [mclass RiskEngine:[m
             [m
             impact = cls.calculate_financial_impact(asset["financial_impact_components"])[m
             eal = cls.calculate_eal(prob, impact)[m
[31m-            risk_score = cls.calculate_risk_score(prob, impact)[m
[32m+[m[32m            risk_score = cls.calculate_risk_score(prob, impact, asset_id=asset.get("id"))[m
 [m
             asset["incident_probability"] = prob[m
             asset["total_financial_impact"] = impact[m
